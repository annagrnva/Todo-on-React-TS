import Button from "@mui/material/Button";

const onClick = () => {};

export const AddButton = () => {
  return (
    <Button variant="outlined" onClick={onClick}>
      Add
    </Button>
  );
};
